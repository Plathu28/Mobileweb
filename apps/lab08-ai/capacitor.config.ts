import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cpkku.metus.lab08', 
  appName: 'lab08-ai', // แนะนำให้แก้เป็นชื่อโปรเจกต์ปัจจุบัน
  webDir: 'dist'
};

export default config;